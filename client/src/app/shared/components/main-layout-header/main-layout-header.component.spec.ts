import {TestBed} from '@angular/core/testing';
import {MainLayoutHeaderComponent} from './main-layout-header.component';
import {Router} from '@angular/router';
import {ProductService} from '../../services/product.service';
import {HttpClient, HttpHandler} from '@angular/common/http';

/*describe('HeaderComponent', () => {
  let app: MainLayoutHeaderComponent;
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MainLayoutHeaderComponent, ProductService, Router, HttpHandler, HttpClient]
    });
    app = TestBed.inject(MainLayoutHeaderComponent);
  });

  it('should create', () => {
    expect(app).toBeDefined();
  });
});*/
